\usepackage[a4paper]{geometry}
\geometry{landscape, margin=1in}

\usepackage[T1]{fontenc}
\usepackage{helvet}

\usepackage{multicol}
\usepackage{xcolor}

% setup for white on black aka dark mode
\usepackage{pagecolor}
\pagecolor{black}
\color{white}

\setlength{\parindent}{0pt}
% \usepackage{mdframed}
% \newcommand{whitebox}[1]{\begin{mdframed}[backgroundcolor=black,linecolor=white,fontcolor=white]#1\end{mdframed} }

% directly lifted from https://blkwll.gumroad.com/l/latextemplate, go check it out
\usepackage[many]{tcolorbox}
\definecolor{w}{HTML}{eeeeee}
\definecolor{g}{HTML}{444444}
\definecolor{b}{HTML}{222222}

\newtcolorbox{conceptbox}[2][]{
	breakable,
	vfill before first=false,
	segmentation at break=false,
	size=fbox,
	colback=b,
	title=\scriptsize\textbf{\MakeUppercase{#2}},
	left=2pt,
	right=2pt,
	top=3pt,
	bottom=1pt,
	boxrule=1pt,
	coltitle=b,
	colupper=w,
	pad at break=5pt,
	toprule at break=4pt,
	bottomrule at break=0.75pt,
	colframe=#1,
	enlargepage=12in, 
	before upper*={\setlength{\baselineskip}{0.75em}\setlength{\parskip}{0em}}
}
% lift ends
